import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ReelsPage } from './reels';

@NgModule({
  declarations: [
    ReelsPage,
  ],
  imports: [
    IonicPageModule.forChild(ReelsPage),
  ],
})
export class ReelsPageModule {}
